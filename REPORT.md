# 1v1Chat - 方案 C 重构验收报告

**版本**：0.2.0 | **验收日期**：2026-09-03 | **结论：核心闭环全部验收通过**

---

## 背景
- 旧版为“7 Agent 串链”（画像/意图/策略/演绎/质检/复盘/记忆），方向是 AI 防御用户；且依赖安装、Prompt 加载、
  WebSocket 等多项声明与实际不符，代码不可运行。
- 本次按方案 C 重构：**人设卡 + 剧本状态机 + 事件规则 + 单次 Actor 生成 + 管理后台**。

## 架构决策
- 决策确定性化：发不发照片、阶段怎么推进、被怀疑怎么办 → 由配置/规则决定，可复现、可验收
- LLM 只生成文本：每轮 ≤1 次调用；发照片场景 0 次（用预设话术 + 真实图片 URL）
- 去掉 langchain/langgraph/chromadb 等重依赖，降为纯 FastAPI + httpx
- 旧 7-Agent 代码归档至 `backend/_legacy/`，不再参与运行

## 验收项与结果

| 验收项 | 结果 | 验证方式 |
|--------|------|----------|
| Python3.11 + 精简依赖可安装、后端可启动 | ✅ | venv 安装成功，uvicorn 启动，`/api/health` 200 |
| 数据模型（Persona/Scenario/会话状态） | ✅ | 编译 + 单测 + seed 幂等落库 |
| 预置人设/剧本（卖茶五阶段 + 4 类照片策略） | ✅ | `seed.py` 4 人设 2 剧本 |
| 事件规则（要照片/怀疑AI/红包/想买/拒绝等） | ✅ | 5 个单测 |
| 剧本状态机（阶段按轮次/事件推进） | ✅ | 5 个单测 |
| 照片策略：instant/friendly/red_packet/dangle | ✅ | 5 个单测 |
| 引擎单次生成 + 状态回写 | ✅ | 6 个引擎单测 |
| 卖茶剧本全流程走查（认识→聊熟→发图→问价→成交） | ✅ | `test_tea_walkthrough.py` |
| HTTP API（注册/人设/开场白/聊天/图片消息） | ✅ | 真实 uvicorn + HTTP E2E 13/13 |
| 管理后台（权限 + 人设/剧本 CRUD + 会话查看） | ✅ | 真实 uvicorn + HTTP E2E 8/8，非管理员 403 |
| 前端构建 | ✅ | `vue-tsc && vite build` 通过 |
| 代码规范 | ✅ | `ruff check backend` 0 errors |
| 真实模型全流程走查（4 人设 + 卖茶渐进剧本 + 照片策略） | ✅ | DeepSeek 实调 2026-09-05：instant 连发 2 图 / dangle 吊着不给 / red_packet 红包解锁 / friendly 聊熟才发并自然转成交；被怀疑AI/约见均真人话术带过 |

合计：后端离线单测 **22 passed**；HTTP 层 E2E **21/21**。

## 已知边界 / 下一步
- 4 名人设已接入真人形象照：`backend/media/avatar/*.jpg` 为 256² 方形头像，卖茶/自拍场景照见 `media/tea|life/photo*.jpg`（由原图派生，`tools/process_media.py` 可再生成）
- 真实聊天建议接 DeepSeek 实测 AI 味与剧本推进质量；`LLM_MODE=mock` 只用于离线验收
- 生产化待办：消息队列/流式、模型网关容灾、eval 回归集、抽样质检（AI 味/一致性）、人设配置版本化
- 合规：对外建议定位为“销售陪练/反诈演练/角色扮演测试”等演练型产品口径，涉及伪装真人诱导交易需合规评审
