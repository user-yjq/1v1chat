# 1v1Chat 文档导航与状态看板

本文档是 1v1Chat 重构（engine2）开发期的**文档总入口**。开发前必须读的文档按顺序为：
`01 → 02 → 03 → 04 → 05`；开发中每完成一步必须同步更新 `05 / 06 / 07`。

## 文档清单

| 编号 | 文档 | 作用 | 维护时机 |
|------|------|------|----------|
| 00 | 本文件：导航与状态看板 | 文档地图、状态、契约 | 每次有变化 |
| 01 | 需求与边界 | 冻结功能/非功能需求、合规口径、明确不做的事 | 需求变更评审时 |
| 02 | 技术选型 | 每个技术决策（ADR）的结论/理由/代价/备选 | 引入新依赖前 |
| 03 | 架构设计 | 分层、节点契约、状态 schema、并发/事务、目录与配置 | 结构变更评审时 |
| 04 | 工程规划 | 里程碑、任务拆分（WBS）、风险登记、节奏 | 规划调整时 |
| 05 | 实施文档 | 开发流程、编码规范、模块拆分、分步执行清单 | 每步开工前对照 |
| 06 | 验收台账 | 每一步的单测/代码/业务/权限/安全/性能边界记录 | 每步完成后登记 |
| 07 | 实施报告 | 按阶段/步骤记录执行过程、变更、风险、遗留 | 每步完成后追加 |
| 08 | 生产化缺口清单与加固台账 | T-15/M4 缺口审计、加固顺序与回填状态 | 每完成一项加固后回填 |

## 版本路线

| 版本 | 含义 | 状态 |
|------|------|------|
| v0.2.0 | 方案 C 基线（确定性状态机 + 单次 Actor），已封存 | ✅ 已验收（2026-09-05 复跑：22 tests / ruff 0） |
| v0.3.0 | engine2 重构 + 双引擎路由与安全加固 | 🚧 M1/M2 完成（T-02~T-13），69 tests 绿 + HTTP E2E 7/7；待 v0.4 真模型调优 |
| v0.4.0 | 评测与真人感调优（试探集 + 对抗评测） | 🚧 离线完成 + 对抗评测工具就绪（16 剧本，mock 冒烟 0 硬失败）；待真模型走查/人工评审 |
| v0.5.0 | 生产化候选（队列/网关/观测/Alembic/安全加固） | 🚧 T-15 缺口清单评审完成（docs/08），待需求评审与加固实施 |

## 核心约定（所有文档共同遵守）

- 引擎代号 `engine2`，与旧 `backend/engine/` 并行，靠配置 `ENGINE_VERSION` 切换（v1=旧 / v2=新），可随时回滚。
- 代码边界：`engine2` 只依赖 `models/`、`db/`、`config` 与 LLM provider 抽象，**不反向依赖路由与旧 engine**。
- LLM 边界：每轮最多 2 次调用（Analyzer 理解 + Actor 说话）；发不发照片、到没到阶段等**决策不进 LLM**。
- 文档契约：文档先于代码；实现与文档冲突时，先改文档与台账记录，再动代码。
- 台账即证据：所有“已完成”必须能给出命令/测试/报告证据，不允许口头结论。

## 关键路径（代码侧）

```
backend/
  main.py                FastAPI 入口（v0.2.0，后续仅加路由开关）
  config.py              配置（engine2 新增项见 03 文档 §配置矩阵）
  engine/                【冻结】方案 C 旧引擎（切 v2 后归档 _legacy/）
  engine2/               【规划】新引擎（schema/pipeline/nodes/policies/tactics）
  services/chat_engine.py   v1 服务（保留）
  services/chat_engine2.py   v2 服务（规划，签名与 v1 对齐）
  routers/               HTTP 层（会话归属与权限在这里校验，engine 不重复实现）
  models/                Persona/Scenario/Conversation/Message/User
  tests/                 v1 测试（冻结，切 v2 前保持绿）
  tests/engine2_core/     v2 测试（已落地；命名避开 engine2 包名冲突）
frontend/                Vue3（API 兼容，v0.3 原则上不改前端）
docs/                    本文档体系
```

## 状态图例

- ✅ 完成且有证据
- 🚧 进行中
- ⏸ 未开始 / 挂起
- ⚠️ 有风险或待决策（需在 07 实施报告记录）
